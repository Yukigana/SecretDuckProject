{% extends '/Layouts/header_and_menu.html.twig' %}

{% block title %}new user{% endblock %}

{% block main_title %}Nouveau compte{% endblock %}

{% block corps %}
    <h2>Formulaire d'inscription</h2>

    {{ form_start(myform) }}

    {{ form_errors(myform) }}

    {{ form_row(myform.libelle) }}

    {{ form_rest(myform) }}

    {{ form_end(myform) }}

    <h2><a href="{{ path('overview_list') }}"><button class="mon-button" > Retour accueil </button></a></h2>

    <h2>dump</h2>
    {{ dump(myform) }}
{% endblock %}